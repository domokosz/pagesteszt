name: CI

on: [push]

jobs:
  build:

    runs-on: self-hosted
    
    steps:
    - uses: actions/checkout@v1
    - name: Run a one-line script
      run: echo Hello, world!
    - name: Run a one-line script
      run: Futtatas!
    - name: dir > dirlista.txt
      run: |
        echo Add other actions to build,
        echo test, and deploy your project.
